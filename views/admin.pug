extends layout

block content
  link(rel="stylesheet", href="/styles/form.css")
  form.count(method='POST' action='' novalidate autocomplete="off")
    h1.titleH1 Submir for be admin 
    button.btnSubmit.btn(type="submit") Submit